const { add, sub, mul, name } = require("./operator");

console.log(`Sum of two numbers:`, add(15, 5));
console.log(`Substraction of two numbers:`, sub(15, 5));
console.log(`Multiplaction of two numbers:`, mul(5, 15));
console.log(name);
